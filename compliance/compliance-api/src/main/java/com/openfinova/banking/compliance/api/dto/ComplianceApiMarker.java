package com.openfinova.banking.compliance.api.dto;

/** Placeholder marker so the compliance-api module publishes a reusable package for future DTO growth. */
public final class ComplianceApiMarker {
    private ComplianceApiMarker() {
    }
}
